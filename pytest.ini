[pytest]
markers =
    slow: marks tests that sleep/wait on real wall-clock time (real server, real tick loop, or real network round trips) - excluded from `pytest -m "not slow"` for fast, deterministic day-to-day runs.
